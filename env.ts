import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'isomorphic-region-loader.example.domain.com',
    title: 'Isomorphic Region Loader',
  },
};
export default env;
